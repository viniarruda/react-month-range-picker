import colors from './colors'
import fontSizes from './fontSizes'
import radii from './radii'
import media from './media'
import space from './space'

const config = {
  name: 'React Month Range Picker Theme',
  fontSizes,
  media,
  radii,
  colors,
  space,
}

export default config
